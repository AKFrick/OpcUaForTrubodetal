<Window x:Class="OpcUaForTrubodetal.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OpcUaForTrubodetal"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <local:MainWindowViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="200"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="tbOutput" Grid.Row="1" 
                   TextWrapping="Wrap"
                   Text="{Binding MyText}"
                   VerticalAlignment="Stretch"
                   HorizontalAlignment="Stretch"
                   Background="AliceBlue"
                   Padding="5,5,5,5"                    
        />
        <Button Content="Button" HorizontalAlignment="Stretch"  Margin="5,5,5,5" VerticalAlignment="Stretch" Command="{Binding MyCommand}"/>


    </Grid>
</Window>
